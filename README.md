This is my last OOP Project (Semester2).

It uses a Qt GUI.


The application works with multiple repositories:
	- In memory repository
	- File repository (CSV)
	- HTML-File repository
	- SQLite3 repository

